import { MainContent, OuterContainer } from "./SharedComponents";

const TeamCreator = () => {
  return (
    <OuterContainer>
      <MainContent>
        <h2>New Team</h2>
      </MainContent>
    </OuterContainer>
  );
};

export default TeamCreator;
